import 'package:flutter/material.dart';

class BioTab extends StatelessWidget {
  const BioTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('紹介文（BIO）をここに表示'),
    );
  }
}
